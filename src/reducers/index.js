const initialState = {
  username: 'fannyhasbi' // example
}

const mainReducer = (state = initialState, action) => {
  switch(action.type){
    default:
      return state;
      break;
  }
}

export default mainReducer;